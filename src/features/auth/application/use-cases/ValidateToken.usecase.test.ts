import { ValidateTokenUseCase } from './ValidateToken.usecase';

describe('Feature: Validate Token', () => {
  let useCase: ValidateTokenUseCase;

  beforeEach(async () => {
    useCase = new ValidateTokenUseCase();
  });

  describe('Scenario: Happy Path', () => {
    const payload = {};

    it('should', async () => {});
  });
});
